import { AppThunkAction } from '..';

export function <%= props.camelCase(props.name) %>Action(payload: <%= props.payloadType %>): AppThunkAction {
  return dispatch => {
    dispatch({ type: '<%= props.type %>', payload })
  }
}
