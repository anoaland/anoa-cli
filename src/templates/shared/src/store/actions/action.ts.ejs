<% if (props.useImport) { %> import { AppThunkAction } from '..'; <% } %>

<%- props.oldFileContents %>

export function <%- props.camelCase(props.name) %>Action(<% if (props.payloadType) {%> payload: <%- props.payloadType %> <% } %>): AppThunkAction {
  return dispatch => {
    dispatch({ type: '<%- props.type %>'<% if (props.payloadType) {%>, payload <% } %>})
  }
}
