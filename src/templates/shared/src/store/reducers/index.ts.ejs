// tslint:disable:ordered-imports

// --------------------------------------------------------- //
// Please do not edit this file. This file is autogenerated. //
// --------------------------------------------------------- //

import { combineReducers } from 'redux'<% props.reducers.forEach(function(r) { %>
import <%= r.val %>Reducer from './<%= r.dir %>'
import <%= r.val %>Actions from './<%= r.dir %>/actions'<% }) %>

export const reducers = combineReducers({<% props.reducers.forEach(function(r) { %>
  <%= r.name %>: <%= r.val %>Reducer,<% }) %>
})
<% if (!props.reducers.length) { %>
export type AppRootActions = any
<% } else { %>
export type AppRootActions = <% props.reducers.forEach(function(r) { %>
  | <%= r.val %>Actions<% }) %>
<%} %>
export type AppRootState = ReturnType<typeof reducers>