type <%= props.name %>Actions = <% props.states.forEach(function(s) { %>
  | {
      type: '<%- props.upperCase(props.name) %>/<%- props.upperCase(s.name) %>'
      payload: <%- s.type %><% if (s.optional) { %> | undefined<% } %>
    }<% }) %>

export default <%= props.name %>Actions