import React from 'react'
import { Text, View } from 'react-native'
import { <%= props.name %>Props } from './props'

export function <%= props.name %>(props: <%= props.name %>Props) {
  return (
    <View>
      <Text><%= props.name %></Text>
    </View>
  )
}
