import React from 'react'
import { Text, View } from 'react-native'
import { <%= props.name %>Props } from './props'

export const <%= props.name %>: React.SFC<<%= props.name %>Props> = (props) => {
  return (
    <View>
      <Text><%= props.name %></Text>
    </View>
  )
}