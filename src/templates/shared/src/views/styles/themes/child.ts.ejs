import { <%- props.from %> } from './<%- props.fromPath %>'

export const <%- props.name %>Theme = <%- props.from %>.extend(  
  {
    // override default theme props value
  },  
  props => ({
    // override default theme styles
  })
)
