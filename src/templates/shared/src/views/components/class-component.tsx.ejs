import React from 'react'
import { Text, View } from 'react-native'
<% if (props.withStore) { %>import { AppStore } from '../../store'<% } %>  
<% if (props.importStatements.length) { %><%- props.importStatements.join('\r\n') %><% } %>

<% if (props.withStore) { %> <% if (props.stateProps) { %>
interface StateProps {
  <%- props.stateProps.join('\r\n') %>
}<% } %>  

<% if (props.actionProps) { %>
  interface ActionProps {
    <%- props.actionProps.join('\r\n') %>
}<% } %><% } %> 

export interface <%- props.name %>Props <% if (props.withStore) { %> extends Partial<<% if (props.stateProps) { %>StateProps<% } %><% if (props.actionProps) { %> <% if (props.stateProps) { %> & <% } %> ActionProps <% } %>> <% } %> {}
<% if (props.withState) { %>
export interface <%- props.name %>State {}
<% } %>

<% if (props.withStore) { %>
@AppStore.withStoreClass<<% if (props.stateProps) { %>StateProps<% } else { %>any<% } %><% if (props.actionProps) { %>, ActionProps <% } %>>(
  <% if (props.stateMap) { %>
  state => ({
    <%- props.stateMap %>
  })
  <% } else { %>
    null
  <% } if (props.actionMap) { %>, dispatch => ({
      <%- props.actionMap %>
    })
  <% } %>

)<% } %>
export class <%- props.name %> extends React.Component<<%- props.name %>Props<% if (props.withState) { %>, <%- props.name %>State<% } %>> {
  constructor(props: <%- props.name %>Props) {
    super(props)<% if (props.withState) { %>
    this.state = {}<% } %>
  }

  public render() {
    return (
      <View>
        <Text><%- props.name %></Text>
      </View>
    )
  }
}
